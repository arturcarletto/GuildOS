# Docker Compose defaults for local development only.
POSTGRES_DB=guildos
POSTGRES_USER=guildos_app
POSTGRES_PASSWORD=guildos_local
POSTGRES_PORT=5432

# Runtime variables used when the local Spring profile is not active.
DB_URL=jdbc:postgresql://localhost:5432/guildos
DB_USERNAME=guildos_app
DB_PASSWORD=guildos_local

# Optional Discord Gateway settings. The integration is disabled by default.
GUILDOS_DISCORD_ENABLED=false
DISCORD_BOT_TOKEN=

# Optional experimental Telegram adapter (proof of concept). Disabled by default.
# When enabled, a bot token from @BotFather is required; never commit a real token.
GUILDOS_TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=

# Optional human operator login. Never commit a real Discord OAuth client secret.
# This root .env file is read by Docker Compose only; a Spring Boot process started
# directly reads these OAuth variables from its shell environment instead.
# When enabled, the login flow requests the "identify" and "guilds" scopes so operators
# can onboard guilds; no additional secrets are required.
GUILDOS_IDENTITY_DISCORD_OAUTH_ENABLED=false
DISCORD_OAUTH_CLIENT_ID=
DISCORD_OAUTH_CLIENT_SECRET=
DISCORD_OAUTH_REDIRECT_URI=http://localhost:8080/login/oauth2/code/discord
# Where the backend sends the browser after a successful Discord login. Defaults to /api/v1/me;
# the local Spring profile already points this at the operator dashboard on the Vite dev server so
# operators land back in the dashboard instead of a JSON endpoint. Override it here if needed.
DISCORD_OAUTH_SUCCESS_REDIRECT_URI=http://localhost:5173/dashboard
